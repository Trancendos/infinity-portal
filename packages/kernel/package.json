{
  "name": "@infinity-os/kernel",
  "version": "0.1.0",
  "description": "Infinity OS Kernel \u2014 Service Worker process manager, IPC, permissions, and event bus",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@infinity-os/types": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@types/node": "^25.3.3",
    "tsx": "^4.6.0"
  }
}
