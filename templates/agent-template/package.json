{
  "name": "@trancendos/{{AGENT_ID}}",
  "version": "0.1.0",
  "description": "{{AGENT_DESCRIPTION}}",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src/ --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["trancendos", "agent", "{{AGENT_ID}}"],
  "author": "Trancendos",
  "license": "MIT",
  "dependencies": {
    "@trancendos/agent-sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2",
    "eslint": "^8.55.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
